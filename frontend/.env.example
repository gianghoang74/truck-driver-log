# Optional. In dev, Vite proxies /api to the Django backend (see vite.config.js),
# so this can be left blank. Set it for production if the API is on another host.
VITE_API_BASE=

# Override the dev proxy target if the backend runs on a non-default host/port.
# VITE_API_TARGET=http://localhost:8000
